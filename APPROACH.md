## Starting point (Ellie's constraint, verbatim from the founder's brief)

Ellie is an old school manager, she doesn't want a new platform. she want to use the one she already knows. (Telegram or Slack)

What I'm thinking is why having a dedicated chat project where people need to go log in and chat, and Ellie will need to go and login and approves. she clearly doesn't like that. and people won't do that either.

"Nobody logged in after week one."

---

## What we're building (v1)

A Slack app/plugin backed by our own service. It does three things:

1. **Owns the catalog.** Persists all ~300 products (SKU-keyed), kept current via CSV re-sync (see below). This is the source of truth, not the spreadsheet itself.
2. **Turns a Shot Idea into candidates, async.** Anyone can open the bot, pick a product from the catalog, and type a shot idea (free text — people already know how to prompt; prompt suggestions are a v2 nice-to-have, not required to unblock this team). The backend calls the Luma API and returns candidate images to that person privately, on their own time.
3. **Leaves approval exactly where it already lives.** The requester picks their preferred candidate(s) and posts them into the existing team chat, the same way anyone would drop a photo in today. Ellie approves the same way she already does — no new buttons, no new step for her. We are not touching her workflow at all in v1; we're only collapsing the freelancer-turnaround bottleneck (steps 3–4 in the README) from weeks to minutes.

New catalog exports (e.g. the 40-product drop) come in via a Slack slash command with the CSV attached — stays inside the chat, no external tool, upserted by SKU so in-flight requests/history aren't lost on re-sync.

## Key decisions and tradeoffs

- **Slack over Telegram.** README left this open; a real engagement would've confirmed it with the customer first. We're assuming Slack for its richer bot/interactivity surface, and treating "we didn't get to ask" as a flag, not a fact — see ASSUMPTIONS.md #1.
- **Decouple generation from approval.** Generation happens in a private, async surface (DM/modal with the bot); approval stays 100% in the existing team chat, untouched. This directly serves "don't burn budget on stuff she'll reject" — a requester can look at candidates before anything reaches Ellie — without adding any new step to *her* side. See ASSUMPTIONS.md #2–3.
- **Structured approve/reject, Ellie-only.** Originally deferred so Ellie's process stayed byte-for-byte what it is today; revisited once real usage showed the untracked "post your pick and hope someone notices" flow wasn't good enough. Now: a requester can explicitly send a candidate to a dedicated approval channel, and only the Slack user in `SLACK_ADMIN_USER_ID` (Ellie) can Approve/Disapprove it — everyone else who clicks gets a private "only Ellie can approve" message and the button state doesn't change. This does add one new step to Ellie's side (clicking a button instead of just reacting in chat informally), a deliberate tradeoff for getting bot-tracked approval state and CSV write-back — see below.
- **Backend, not the sheet, is the source of truth.** CSV is an import format, not the live datastore — upsert by SKU on each sync. See ASSUMPTIONS.md #4.
- **CSV re-sync via Slack slash command + file upload.** Keeps the one admin action that isn't "approve a photo" still inside chat. See ASSUMPTIONS.md #7.

## Scope ledger

**In (v1):**
- Slack app: product picker + shot-idea capture, async/private to the requester.
- Luma generation, 2–3 candidates per request (matches README's definition of "done").
- Requester manually posts chosen candidate(s) to the team chat — no automation here, matches today's behavior exactly.
- Catalog persistence + CSV re-sync via slash command, upsert by SKU.
- Deployed, live instance (not localhost).
- **Slack App Home tab** (`src/slack/home.ts`) — read-only status list of the 20 most recent shot requests (product, shot idea, status, requester, timestamp), rendered when anyone opens the app's Home tab. Answers Maya's "see status without asking Ellie" ask, pulled forward from the "Next" list below because it's still 100% inside Slack (no new platform/login, works on phone) — unlike a separate dashboard, which we explicitly rejected as violating the "if it's not in the chat it doesn't exist" constraint. Requires `app_home_opened` in the manifest's `bot_events` and `features.app_home.home_tab_enabled: true` (both added to `slack-app-manifest.json`) — not live until the manifest is pushed/reinstalled per `SETUP.md` §3.
- **Approve/Disapprove (Ellie-only) + accepted prompts + CSV write-back.** See Build status below for the full design; summarized here as scope: a "Send to Ellie for approval" button per candidate, an approval channel with Approve/Disapprove buttons gated to one Slack user, a new `AcceptedPrompt` table (replacing the old single `Product.shotIdea` column) so a product can hold multiple accepted prompts, and an automatic rewrite of `data/catalog.csv` on every approval.

**Out / deferred to v2 — logged here because it was explicitly cut, not forgotten:**
- **Prompt suggestions** for the shot-idea field. Cut because the bottleneck this team described is turnaround time, not prompt quality — people already write reasonable shot ideas unassisted today.
- **Auto-write approved images to the product page / shared drive.** Approved shot ideas now write back to `data/catalog.csv` automatically (see Build status), but that's the *catalog*, not the product page or the shared drive folder the real photo assets live in — that hop is still manual, same as today.
- Automatic/webhook-based CSV ingestion (polling Drive/Sheets) instead of a manual slash-command upload.

**Next (not started, candidate order):**
1. Write-back to the drive folder / product page once something's approved (the CSV write-back above covers the catalog record, not the actual image asset going where the team already looks for it).
2. Prompt suggestions.
3. Durable CSV export — the current write-back targets local container disk, which Railway wipes on every redeploy (see Build status "known limitation"). Worth an on-demand `/catalog-export` (upload the current CSV as a Slack file) as a durable fallback once real usage shows the auto-write isn't enough on its own.

## Build status

**Backend is built and verified locally.** Node.js + TypeScript (strict) + Express, PostgreSQL via Prisma (chosen over a JSON-file store for real scalability — durable, migratable, handles 10x catalog growth), Slack via `@slack/bolt` (mounted only once `SLACK_BOT_TOKEN`/`SLACK_SIGNING_SECRET` are set, so the REST API works standalone before a Slack app exists).

**Run it:** see `SETUP.md` for the full walkthrough (env vars, Docker vs local, Slack app creation/manifest-update steps). Short version: `cp .env.example .env` (fill in Slack creds later, safe to leave blank for now) → `docker compose up --build` from the repo root. Starts Postgres, runs Prisma migrations, imports `data/catalog.csv`, serves on `:3000` (`GET /`, `GET /health`, `GET /api/products[/:sku]`, `POST /api/catalog/sync`, `POST /api/requests`, `GET /api/requests/:id`, plus `/slack/*` once Slack env vars are set). Local dev without Docker: `npm install && npx prisma migrate dev && npm run dev` (needs a reachable Postgres via `DATABASE_URL`).

**Setup instructions live in `SETUP.md`, not here.** Pulled the "how to run this" walkthrough (env, Docker, Slack app creation, and — new — the manifest-update/reinstall flow for pushing `slack-app-manifest.yaml` changes to an existing Slack app) into a dedicated file so APPROACH.md stays focused on decisions/status rather than growing into a setup doc. Added because the `im:history`/`message.im` manifest change (above) needs a manual "paste manifest, reinstall to pick up new scopes" step that didn't have a home before.

Implemented: catalog CRUD + CSV upsert-by-SKU sync, the full request lifecycle (`POST /api/requests` → `GET /api/requests/:id`), and Slack handlers (`/shot-request` modal → DM with result; `/catalog-sync` → prompts for a file share, synced via a `message` event listener since Slack slash commands can't carry attachments — a wrinkle the original plan glossed over). Verified end-to-end against the sample 40-product CSV: health check, product list/lookup, idempotent re-sync, and a full create-request round trip.

**Generation is live** (`src/generation/lumaClient.ts`) — calls the real Luma Agents API (`POST /v1/generations`, `type: "image_edit"`, `model: "uni-1"`), polls `GET /v1/generations/{id}` every 2s up to a 2-minute timeout, per docs.agents.lumalabs.ai. The API returns one image per call, so "2–3 candidates" (scope ledger) means firing 2 parallel `image_edit` calls per request and merging their outputs — not a single call with a count param, since the API doesn't support that. If some candidates fail and others succeed, we still return the successful ones; only an all-fail case marks the request `failed`. Slack DM now renders every candidate image, not just the first. Requires `LUMA_AGENTS_API_KEY` set (Railway + local `.env`); the function signature/contract from the earlier stub was preserved so no upstream code (request service, routes, Slack handlers) needed to change beyond rendering multiple images.

**Repo layout: backend lives at repo root, not `backend/`.** Originally built under `backend/`, but Railway's GitHub-integration deploy builds from the repo root by default and doesn't auto-detect a Dockerfile in a subdirectory — it needs "Root Directory" set explicitly in the dashboard, and the first deploy failed without it. Moving `package.json`/`Dockerfile`/`docker-compose.yml`/`src/`/`prisma/` to the repo root sidesteps that entirely: Railway (and anyone else) finds the Dockerfile where it looks by default. `data/catalog.csv` was already at the repo root, so `CATALOG_CSV_PATH=./data/catalog.csv` needed no change. Re-verified end-to-end after the move (`docker compose up --build` from root, health check + product list + request round-trip all still pass).

**Catalog sync moved to DM, not channel.** Originally the plan had `/catalog-sync` + file-share detection happening in a shared team channel (`message.channels` event). Product call: only one person maintains the catalog, so there's no reason to broadcast the raw CSV/sync noise to the whole team channel — a private DM with the bot is the right surface. Since our `message` event listener in `src/slack/commands.ts` already handles any message type generically (channel, DM, group — Slack just dispatches whichever subscribed event type occurred), no code changed on our side — the outstanding piece is Slack app config: add the `im:history` bot scope and subscribe to the `message.im` bot event (alongside the existing `message.channels`), then reinstall.

**Candidate images now post as separate messages, and prompts are reusable across requesters.** Two changes to `src/slack/commands.ts`, both prompted by direct feedback that the DM result was awkward to use:
- **One Slack message per candidate image**, not one message with N image blocks. Forwarding/sharing a specific candidate previously meant forwarding the whole bundle (summary text + every image); now each candidate is its own message so it can be forwarded on its own. Summary text and the "post your pick" note are still separate single messages around the image messages.
- **Reuse-a-prompt in the `/shot-request` modal.** Past shot ideas were already sitting unused in `GenerationRequest.shotIdea` (per SKU). Added `listRecentShotIdeasForSku` (`src/requests/service.ts`, dedupes by exact text, most-recent-first, capped at 5) and wired the modal to re-render itself when a product is picked (`dispatch_action` on the product `static_select`, handled via `client.views.update` in a new `product_select` block-action listener) showing an optional "reuse a previous idea" dropdown labeled with who wrote each one. Picking one fills the shot-idea textbox (`reuse_prompt_select` handler) but stays editable — this is a starting point, not a lock-in. Chose modal-state round-tripping over a separate lookup command because it keeps everything inside the existing one-form flow Ellie/requesters already use, no new command to learn.

**Manifest format switched YAML → JSON, and reset to mirror the actually-deployed app.** `slack-app-manifest.yaml` → `slack-app-manifest.json`. While diffing, found the repo's manifest had drifted from what's live on Slack (repo said "Styled Shots" / had `im:history` + `message.im` already; live app is "Lumalabs Bot" and has neither) — the repo file was apparently edited ahead of ever being pushed to Slack, so it documented a change that hadn't actually shipped. Reset `slack-app-manifest.json` to match the live app exactly, so the repo reflects deployed reality instead of aspiration. Net effect: **DM-based catalog sync is not live yet** — `im:history`/`message.im` still need to be added to the manifest and pushed via the manual reinstall flow (`SETUP.md` §3) before it'll work in production.

**App Home tab added** (`src/slack/home.ts`) — publishes a `home`-type view via `views.publish` on `app_home_opened`, listing the 20 most recent `GenerationRequest` rows (new `listRecentRequests` in `src/requests/service.ts`) with product, shot idea, status emoji, requester, and timestamp. Considered putting the `/shot-request` flow itself in the Home tab instead of the existing modal, but modals are the right surface for a one-time form submission — Home tab is for glanceable status, not data entry, so `/shot-request` is untouched. Needs `features.app_home.home_tab_enabled` and `app_home_opened` in `bot_events`, both added to `slack-app-manifest.json` — bundled with the same pending manifest push below.

Not yet done: pushing the updated `slack-app-manifest.json` (`im:history`, `message.im`, `app_home_opened`, `features.app_home`) to Slack and reinstalling (`SETUP.md` §3), then confirming DM-based catalog sync and the Home tab end-to-end.

<<<<<<< HEAD
**Bug found in production: `/shot-request` completes generation but fails to deliver images.** Reported symptom: bot posts "Generating..." then "Status: completed", then immediately "Something went wrong generating that shot — try again in a bit." Two stacked causes, both now fixed:
1. `slack-app-manifest.json`'s bot scopes had `files:read` but not `files:write`, which `client.files.uploadV2` (`src/slack/files.ts`) requires. Fixed by adding `files:write` to the manifest — required a manual reinstall via `SETUP.md` §3, which surfaced the real bug underneath.
2. Even after the scope fix and reinstall, uploads still failed: `files.uploadV2`'s `channel_id` must match `^[CGDZ][A-Z0-9]{8,}$` (an actual conversation ID) — unlike `chat.postMessage`, it does **not** accept a raw user ID and silently open a DM. `commands.ts` was passing `requestedBy` (a `U...` user ID) straight through as `channelId`, so every upload was rejected with `invalid_arguments` regardless of scopes. Fixed in `src/slack/commands.ts`'s `SHOT_REQUEST_CALLBACK_ID` handler by resolving the DM via `client.conversations.open({ users: requestedBy })` once up front and using that channel ID (`D...`) for the upload calls. `chat.postMessage` calls elsewhere in the same handler were left as-is (that API does accept a user ID). That `conversations.open` call itself then hit `missing_scope` (`needed: im:write`) — added `im:write` to the manifest's bot scopes.
3. A third bug, found after both of the above were fixed and deployed: submitting the modal produced total silence with no error logged at all. Root cause was the `if (!sku || !shotIdea) return;` guard in the view-submission handler exiting with no log and no user-facing message — first fixed by adding a log line + a "couldn't read your submission" DM, which then surfaced the real underlying issue: `sku` was present but `shotIdea` came back empty specifically when the requester had picked a prompt from the "reuse a previous idea" or "use an approved prompt" dropdown. Slack does not reliably reflect a `plain_text_input`'s `initial_value` (set via `views.update` when those dropdowns re-render the modal) back in `view.state.values` on submission unless the user manually edits the field afterward — a known Slack client quirk, not a bug in our state-reading code. Fixed by stashing the prefilled text server-side in the modal's `private_metadata` (`prefilledShotIdea`, set in `buildShotRequestView`) and falling back to it in the submit handler when the live field value is empty.
4. A fourth bug — the real one underlying both the "second image" and "no Send to Ellie button" symptoms: `src/slack/files.ts`'s `uploadBuffer` was reading `client.files.uploadV2()`'s response one level too shallow. Per the installed `@slack/web-api` source (`WebClient.js` `filesUploadV2()`/`completeFileUploads()`), `uploadV2()` returns `{ ok, files: completion }` where `completion` is the array of raw `files.completeUploadExternal` responses — and *each of those* is itself `{ ok, files: [...] }`, so the real file object is at `result.files[0].files[0]`, not `result.files[0]`. Our code read `result.files[0]` directly, so `uploaded.id` was always `undefined` — it just happened to throw *after* Slack had already accepted and posted the file (the upload itself is a real network call independent of how we parse the result), which is why candidate 1's image would appear in the DM before the whole loop died — aborting before candidate 2 was even attempted, and before candidate 1's own "Send to Ellie" button (posted later in the same loop iteration) ever got sent. First (incorrectly) diagnosed as a Slack processing-delay timing issue and "fixed" by polling `client.files.info` — that polling logic is harmless and stays (permalink/url_private genuinely can lag id by a beat), but it wasn't the actual bug. Real fix: unwrap one more level (`result.files?.[0]?.files?.[0]`).
=======
**Bug found in production: `/shot-request` completes generation but fails to deliver images.** Reported symptom: bot posts "Generating..." then "Status: completed", then immediately "Something went wrong generating that shot — try again in a bit." Root cause: `slack-app-manifest.json`'s bot scopes had `files:read` but not `files:write`; `uploadImageFromUrl` (`src/slack/files.ts`) calls `client.files.uploadV2` to re-upload each Luma candidate as a durable Slack file (see the "Candidate images are re-uploaded to Slack instead of hotlinked" entry above), which requires `files:write`. Without it Slack rejects the upload with `missing_scope`, the loop in `commands.ts` throws, and the outer catch swallows the real error into a generic message — so the failure looked like a generation problem when generation had actually already succeeded. Fixed by adding `files:write` to the manifest's bot scopes; **not live until pushed + reinstalled**, same manual step as the pending `im:history`/`message.im` change — see `SETUP.md` §3.
>>>>>>> f96ba9bda9bf42b9d7572adb827f805ed3c17bbd

**Approve/Disapprove (Ellie-only), accepted prompts, and CSV write-back — built and verified against a local Postgres, not yet deployed.** Replaces the single `Product.shotIdea` column with a new `AcceptedPrompt` table (`sku`, `shotIdea`, `imageUrl?`, `source: "csv"|"slack"`, `approvedBy`, `requestId?`, unique on `(sku, shotIdea)` so re-approving/re-syncing the same text is a no-op) since a product can now hold more than one accepted prompt — the old column could only hold one.
- **Approval surface:** each per-candidate DM message (`src/slack/commands.ts`) now has a "Send to Ellie for approval" button. Clicking it (the requester, in their own DM, no permission check needed) posts that candidate into a new `SLACK_APPROVAL_CHANNEL_ID` channel with Approve/Disapprove buttons, and swaps the DM button out for a static "✅ Sent to Ellie" state — that swap *is* the double-send guard, not a separate DB flag. This preserves the existing private-DM triage step (the requester still decides what's worth Ellie's time) instead of auto-posting every candidate.
- **Ellie-only gating:** `approve_prompt`/`disapprove_prompt` handlers check `body.user.id === env.slackAdminUserId` (new optional env var, single approver, following the same optional pattern as `SLACK_BOT_TOKEN`/`SLACK_SIGNING_SECRET` so the app still boots without it set). Anyone else clicking gets a `chat.postEphemeral` "only Ellie can approve" and the message is unchanged. Approving creates the `AcceptedPrompt` row and rewrites the CSV (below); disapproving just marks the message rejected, no DB write. Gate is togglable via `SLACK_RESTRICT_APPROVAL_TO_ADMIN` — **defaults `false`/unrestricted** (deliberately, so the flow is easy to exercise end-to-end without needing to click as Ellie's actual Slack account during testing/demo); set `true` to actually enforce the Ellie-only restriction, which is required before any real deploy.
- **Modal shows approved prompts too, separately from "tried" prompts.** Picking a product in `/shot-request` now shows two optional dropdowns: the existing "reuse a previous idea" (any past attempt, from last session) and a new "use a prompt already approved for this product" (from `AcceptedPrompt`, via `listAcceptedPromptsForSku` in `src/catalog/acceptedPrompts.ts`) — kept separate rather than merged, since "someone tried this" and "this already won approval" are different signals worth distinguishing.
- **CSV write-back is automatic** (`src/catalog/csvWriter.ts`, new `csv-stringify` dependency): every approval rewrites the entire `data/catalog.csv` from current DB state — one row per product per accepted prompt, or one row with an empty `Shot Idea` for products with none, so the file stays a complete, round-trippable catalog through the existing `parseCatalogCsv`. **Known limitation, accepted deliberately:** this writes to the container's local disk, which Railway wipes on every redeploy. The `AcceptedPrompt` table is the real source of truth; the CSV write is a best-effort convenience export, not durable storage — logged in the "Next" list above as a follow-up (an on-demand Slack-upload export would survive redeploys, this doesn't).
- **CSV import now feeds `AcceptedPrompt`, not `Product`.** Both the boot-time import (`importInitialCatalog` in `src/server.ts` — an existing code path the earlier design pass had missed entirely, caught by testing against the running container rather than static analysis) and `/catalog-sync` now call `upsertAcceptedPromptsFromCsv` for any non-empty `Shot Idea` cell, source `"csv"`, `approvedBy: "csv-import"`. Verified idempotent: restarting the container twice against the same CSV still leaves exactly 16 `AcceptedPrompt` rows, no duplicates.
- **Migration + backfill:** `prisma/migrations/20260813120000_accepted_prompts/` — hand-written (no interactive `prisma migrate dev` session available), then validated for real via `prisma migrate deploy` against the project's own `docker compose` Postgres rather than trusted blind. Includes a backfill `INSERT ... SELECT` that copies any pre-existing `Product.shotIdea` into `AcceptedPrompt` (`source: "csv"`, `approvedBy: "csv-import"`, `imageUrl: null`) before dropping the column — confirmed all 16 of the sample catalog's existing shot ideas (e.g. HG-002's) landed correctly.
- Also fixed in passing: `src/slack/home.ts`'s status-emoji map had a stale `"ready"` key that never matched (`GenerationRequest.status` is actually `"completed"`), so finished requests were showing a bare bullet instead of ✅ on the Home tab.

**Candidate images are re-uploaded to Slack instead of hotlinked.** Luma's generation output URLs are temporary/signed and eventually stop resolving, which would silently break every image reference stored anywhere (DM history, `GenerationRequest.outputs`, `AcceptedPrompt.imageUrl`) once it expired. New `src/slack/files.ts`: `uploadImageFromUrl` downloads the Luma URL's bytes immediately (before it can expire) and re-uploads via `files.uploadV2` as a real Slack file in the requester's DM; `reuploadSlackFile` re-downloads an already-uploaded Slack file (via the bot's own auth) to re-share a fresh copy into another channel, used when "Send to Ellie" fires so the approval-channel copy doesn't depend on the DM copy staying reachable either. `GenerationRequest.outputs` (new `updateRequestOutputs` in `src/requests/service.ts`) gets overwritten with the durable `{fileId, permalink, urlPrivate}` right after upload, and `AcceptedPrompt.imageUrl` now stores the Slack `permalink` (stable, human-viewable) rather than the raw Luma URL. Side effect: candidates now arrive as two messages each (the native file-share message, then a small "Send to Ellie" button message) instead of one, since Slack file-share messages can't carry interactive Block Kit elements.

**`SLACK_RESTRICT_APPROVAL_TO_ADMIN` toggle added for testing.** Defaults `false` (unrestricted — anyone can Approve/Disapprove) specifically so the flow is easy to exercise end-to-end without needing Ellie's actual Slack account during testing/demo; set `true` to enforce the real Ellie-only gate, which is required before any actual deploy — flagging this loudly since an unrestricted default is exactly the kind of thing that's easy to forget to flip.

**`CATALOG_CSV_PATH` env var removed; boot-time file import dropped.** Since the only supported way to get products into the DB is now a Slack-uploaded CSV (`/catalog-sync` or a DM'd file), there was no longer a reason for a *separate*, independently-configurable "path to seed the DB from" — `importInitialCatalog()` in `src/server.ts` (which read that path at every boot) is deleted outright, not just its env var. `data/catalog.csv` still exists in the repo as a sample/format reference and remains the (hardcoded, no-longer-configurable) target of the CSV write-back in `src/catalog/csvWriter.ts` — that's unaffected. **Consequence:** the DB starts completely empty on a fresh deploy/restart now; someone has to DM the bot a CSV before `/shot-request` or the Home tab have anything to show. Acceptable for this exercise's flow (matches "everything happens via chat, nothing pre-seeded behind the scenes"), but worth knowing before demoing a fresh environment — DM a CSV first.

Not yet done: setting `SLACK_ADMIN_USER_ID`, `SLACK_APPROVAL_CHANNEL_ID`, and `SLACK_RESTRICT_APPROVAL_TO_ADMIN=true` in Railway/`.env` (documented in `.env.example`), and the Slack manual walkthrough end-to-end once the pending manifest push (above) is live.

**UI bug: reuse/approved-prompt dropdown options rendered garbled for long or multi-line shot ideas.** `buildShotRequestView` (`src/slack/commands.ts`) built each dropdown option's text as `` `"${shotIdea}" — <@${requestedBy}>`.slice(0, 75) `` (Slack's select-option text cap). Two problems: multi-line shot ideas (containing a literal `\n`) wrapped mid-option instead of showing on one line, and the blind 75-char slice sometimes cut straight through the trailing `<@USERID>` mention tag, leaving broken raw text (`<@U`) instead of Slack's rendered mention pill. Fixed with a `truncateIdeaForOption` helper that collapses whitespace/newlines to a single line and truncates only the free-text idea portion, reserving room for the mention suffix so it's never appended in a partial/broken state.

## Unit economics

Per docs.agents.lumalabs.ai pricing: `uni-1` image editing is **$0.0434/image**, `uni-1-max` is **$0.1030/image**. We default to `uni-1` (cheaper; `uni-1-max` is an easy env-level swap in `lumaClient.ts` if quality demands it).

- **Per request (2 candidates, uni-1): ~$0.0868.** This is the real cost floor of every `/shot-request` submission, since both candidates fire in parallel unconditionally — there's no cheaper single-candidate path today.
- **40-product drop, one shot idea each: ~$3.47** if every product gets exactly one request. Real usage will be higher since requesters can re-run a shot idea if they don't like either candidate.
- **Where budget actually leaks:** not per-image cost, but *repeat requests* — a requester unhappy with both candidates just runs `/shot-request` again, doubling cost with no guardrail today. Nothing currently caps retries per SKU or warns a requester that they're about to spend again. Worth a v2 guardrail (e.g. show a re-generate confirmation, or track spend per SKU) once real usage data exists.

## What breaks first under pressure

*To be filled in as the build progresses — will cover the 10× catalog scenario and where the async-request / manual-post design is most likely to strain.*

## Deployment

*Live URL / Slack workspace link goes here once deployed.*
