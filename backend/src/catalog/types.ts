export interface ProductInput {
  sku: string;
  name: string;
  category?: string;
  color?: string;
  material?: string;
  price?: string;
  photoUrl: string;
  shotIdea?: string;
  notes?: string;
}
